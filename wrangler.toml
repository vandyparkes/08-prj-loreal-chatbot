# Cloudflare Worker — deploy with: npm run worker:deploy
# Before first deploy, set the secret in the dashboard (Workers → your worker → Settings → Variables and Secrets)
# or run: npx wrangler secret put OPENAI_API_KEY

name = "snowy-water-5575"
main = "RESOURCE_cloudflare-worker.js"
compatibility_date = "2024-12-01"
